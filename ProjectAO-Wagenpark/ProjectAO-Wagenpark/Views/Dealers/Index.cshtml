@model IEnumerable<ProjectAO_Wagenpark.Models.Dealer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DealerNR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DealerNR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>

            @if (User.IsInRole("Admin"))
            {
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.DealerNR }) |
                    @Html.ActionLink("Details", "Details", new { id = item.DealerNR }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.DealerNR })
                </td>
            }
            else
            {
                <td>
                    @Html.ActionLink("Details", "Details", new { id = item.DealerNR })
                </td>
            }
        </tr>
    }

</table>
